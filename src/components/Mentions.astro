<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<div id="container">
    <div id="ui-layer">

    <div class="upload-zone">
        <span id="fileName">
            <span><a href="https://github.com/Max-Kawula/penger-obj" class="lien-model" target="_blank">Modele du penguin.<a/></span>
            <span>Author: Max-Kawula.</span>
            <span>Licence: Penger Public Licence.</span>
    </div>
</div>

<style>
    * {
    box-sizing: border-box;
}

#fileName {
    display: flex;
    flex-direction: column;
    line-height: 20px;
}

body, html {
    margin: 0;
    padding: 0;
    width: 100%;
    height: 100%;
    background-color: #000;
    overflow: hidden;
    font-family: sans-serif;
    color: white;
}

#ui-layer {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    width: 90%;
    max-width: 700px;
    z-index: 10;

    background: rgba(20, 20, 20, 0.6);
    backdrop-filter: blur(12px); /* Effet verre */
    padding: 20px 30px;
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 10px 40px rgba(0,0,0,0.6);

    display: flex;
    flex-direction: column;
    gap: 15px;
}

.lien-model {
    font-style: italic;
    font-weight: bold;
}
.lien-model:hover {
    text-decoration: underline;
}

.upload-zone {
    display: flex;
    align-items: center;
    gap: 15px;
    font-size: 0.9rem;
    color: #aaa;
}

.controls-container {
    display: flex;
    align-items: center;
    gap: 20px;
}

/* Boutons */
.btn {
    background: rgba(255, 255, 255, 0.1);
    border: none;
    color: white;
    cursor: pointer;
    transition: all 0.2s;

}
.btn:hover { background: rgba(255, 255, 255, 0.25); transform: scale(1.05); }
.btn:active { transform: scale(0.95); }

.upload-btn {
    padding: 8px 16px;
    border-radius: 20px;
    font-weight: bold;
}

.icon-btn {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    font-size: 1.2rem;
    display: flex;
    justify-content: center;
    align-items: center;
}
.icon-btn:disabled { opacity: 0.3; cursor: not-allowed; }

/* Timeline */
.timeline-wrapper {
    flex-grow: 1;
    display: flex;
    align-items: center;
    gap: 10px;
    font-family: monospace;
    font-size: 0.9rem;
}

input[type=range] {
    flex-grow: 1;
    cursor: pointer;
    accent-color: #ffffff; /* Couleur de la barre sur Chrome/Edge */
}
/* Input file caché */
input[type="file"] { display: none; }

input[type="range"]::-webkit-slider-thumb {
  -webkit-appearance: none;
  visibility: hidden;
}

input[type="range"]::-moz-range-thumb {
  visibility: hidden;
}
</style>
